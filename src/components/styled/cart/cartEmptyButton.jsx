import styled from "styled-components";
import BasicButton from "../button";

export const CartEmptyButton = styled(BasicButton)`
  background-color: grey;
  font-size: 0.9em;
  padding: 5px;
  margin: auto;
`;